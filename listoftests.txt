---- FILE CONTAINS TEST CASES from Holberton Repo  ----

-- betty checker

build betty checker for all .c and .h files

-- tabs

"/bin/ls
     /bin/ls
/bin/ls     
  /bin/ls     "

-- 3 lines

  "/bin/ls
/bin/ls
/bin/ls"

-- long

"                                       /bin/ls                                                                                                                                                                                                                                                                                                                                                                                                                                                     "

-- no-getline
echo "$command" | $LTRACE -bc -o $LTRACEOUTPUTFILE $SHELL > $OUTPUTFILE 2> /dev/null
nmatch=`cat $LTRACEOUTPUTFILE | grep getline | wc -l`

-- ls with args
"/bin/ls -la"

-- Empty the PATH and check if /bin/ls still works and 'ls' same test for both

OLDPATH="$PATH"
PATH=""
# send commands
$ECHO "$command" | $SHELL > $OUTPUTFILE 2> $ERROROUTPUTFILE &

# put PATH back
PATH="$OLDPATH"

-- simple ls

ls

-- mix

"ls
ls
ls
/bin/ls
ls
ls
ls
/bin/ls
ls
ls"

-- mix and spaces

"ls


 /bin/ls 


     ls
ls     
 ls     "

-- check correct order of PATH, create dummy ls file in directory

OLDPATH="$PATH"
PATH=":$PATH"
# send commands
$ECHO "$command" | $SHELL > $OUTPUTFILE 2> $ERROROUTPUTFILE &

# put PATH back
PATH="$OLDPATH"

-- ls should not be found if the PATH is empty
